# === 🚀 FastAPI Framework ===
fastapi==0.110.0
uvicorn[standard]==0.29.0       # ASGI server with logging, reloading, websockets

# === 🗄️ Database & ORM ===
sqlalchemy==2.0.30              # Modern, async-capable ORM
psycopg2-binary==2.9.9          # PostgreSQL adapter
python-dotenv==1.0.1            # .env config
alembic==1.11.2                 # Database migrations

# === 🔐 Authentication & Security ===
passlib[bcrypt]==1.7.4          # Secure password hashing
python-jose==3.3.0              # JWT handling
bcrypt==4.1.2                   # To fix `bcrypt.__about__` bug in Windows

# === 🧠 Data Validation & Settings ===
pydantic==2.7.1
pydantic-settings==2.2.1        # Environment & app config via Pydantic

# === ✉️ Email (Forgot Password / Notifications) ===
aiosmtplib==3.0.1               # Async email sending (Optional, modern)
email-validator==2.1.0.post1    # Strict email validation

# === 🧪 Testing (Optional but recommended) ===
httpx==0.27.0                   # For HTTP requests and testing APIs
pytest==8.2.1                   # Test runner
pytest-asyncio==0.23.6          # Async test support

# === 🧳 Deployment (WSGI/ASGI Server) ===
gunicorn==21.2.0                # Production server (if using w/ Docker)
uvloop==0.17.0                  # Faster event loop for production

# === ✨ Developer Experience (Optional) ===
black==24.4.2                   # Code formatter
isort==5.13.2                   # Import sorter